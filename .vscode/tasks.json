{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate OpenAPI documentation",
            "type": "shell",
            "command": "dotnet swagger tofile --output productionTrackerApi.json bin/Debug/net9.0/productionTrackerApi.dll v1",
            "options": {"cwd": "${workspaceFolder}/backend/productionTrackerApi"}
        }
    ]
}